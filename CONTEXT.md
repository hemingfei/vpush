# vpush 领域术语表

个人投资信息订阅站：聚合 MX 智囊团快讯与多空研判、财经新闻，按用户订阅推送。本文件只收录领域词汇约定，不写实现。

## Language

### 观点研判（已有域）

**智囊团**：
`/mx-views` 页面的别名，指 MX 平台大V（KOL）观点研判板块。
_Avoid_: 军，MX 观点

**大V (KOL)**：
MX 平台中被研判其发言的投资意见领袖，对应 MX 房间成员。

**标的 (Target)**：
一条观点所指向的对象，分为**个股**与**题材**两类。

**个股**：
A股上市公司；名称经全市场名单归一化后才可参与研判与匹配。

**题材**：
板块/概念类主题（如「AI算力」），来源于内置词表与研判产出，开放集合、无权威名单。持股研判页面向用户显示为「板块」。

**观点 (Opinion)**：
LLM 从快讯窗口研判出的结构化多空判断，含标的、方向、摘要与依据。
_Avoid_: 研判结果，消息

**依据原帖 (Evidence)**：
支撑一条观点的原始快讯消息，一条观点可引用多条。
_Avoid_: 来源帖，引用

**多空方向 (Direction)**：
观点对标的的态度：看多（bull）、看空（bear）、中性（neutral）。

**快照 (Snapshot)**：
某交易日在某时刻对全天观点流的整页聚合存档；观点研判页可沿快照时间轴回看。

### 持股研判（新域）

**用户持股 (Holding)**：
单个用户标记为自持的标的（个股或题材），按用户隔离；仅用于筛选该用户的相关观点，与 KOL 雪球组合的持仓无关。
_Avoid_: 持仓（已专指 KOL 组合持仓），自选股

**相关观点**：
标的命中某用户「用户持股」集合的观点；个股按归一化名称精确匹配，题材按名称精确匹配。

**相关快讯**：
标签体系命中标的的快讯帖——posts 标签精确含标的名（规则/LLM/观点回流打标同列），与「相关观点」并列的第二条流；方向角标取观点回流登记。

**一个月窗口**：
从当前时刻往前滚动的 30 个自然日；持股研判页只展示窗口内的相关观点，无快照/日期切换概念。
_Avoid_: 当月，自然月

### 标签审核（新域）

**待审标签**：
打标环节没有把握（低准确度、名单外个股名）的标签，先挂上消息提示性展示，审核定局前不正式计入消息标签。
_Avoid_: 候选标签，临时标签

**大众评审**：
普通用户点开待审标签直接投票（通过/拒绝）的机制；管理员可在后台关闭。一人一票、不可改投。
_Avoid_: 众裁，投票（泛指）

**一致裁决**：
投票人数达到「一致裁决人数」（后台可配，默认 2）且全部同向时，立即按该方向定局；票型分裂则继续收集更多用户的投票。

**最终裁决**：
投票人数达到「最终裁决人数」（后台可配，默认 10）时强制定局，按多数票判定；平票判拒绝——标签上帖是持久写入，不因平票通过。
_Avoid_: 半数规则

**直判**：
管理员在审核弹窗（或后台审核队列）对单条待审标签直接通过/拒绝，立即生效，不经过大众票型。

### 大V消息操作标注（新域）

**操作标注**：
授权用户/管理员对单条 MX 大V消息人工标注「某个股的某个操作」（建仓/加仓/低吸/减仓/高抛/清仓）或「非操作」，修正预估持仓/盈亏回放的漏判误判（如清仓未被识别）。标注对象是（消息， 个股）对，一人一帖一标、可改标覆盖（与大众评审的一人一票不可改投不同——标注是当前判断）。落库表 mx_action_marks，生效与否不落库：回放每次请求按标注与配置现算。
_Avoid_: 打标（泛指 LLM 打标），纠错

**一致生效**：
操作标注的生效规则：同一消息上标注出相同「股票+操作」的授权用户人数达到后台配置的一致人数（默认 2，区间 2~10）即生效；出现两个及以上不同达标组（分歧）则不生效，等管理员定夺。管理员标注不受此限，直判立即生效（多管理员不一致时最近更新者优先）。
_Avoid_: 多数决（本域无「最终裁决」兜底档）

**标注白名单**：
管理后台配置的、拥有操作标注权限的用户名列表（settings 键 mx_action_mark_users）；空列表时仅管理员可标注。被移出白名单的旧标注悬置不计数（标注行保留），重新授权即恢复。

**人工事件（manual）**：
生效的操作词标注注入回放的事件（source=manual），优先级高于观点研判与标签两个自动源：同帖同标的的自动信号被压制，「非操作」标注只压制不注入。清仓后其他帖的建仓信号仍可正常重开仓。
_Avoid_: 手工信号
