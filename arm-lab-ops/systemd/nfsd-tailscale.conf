# Drop-in for /etc/nfs.conf.d/ on Oracle-SJ-ARM.
# nfsd listens on the Tailscale IPv4 only. Export ACL is still
# /etc/exports.d/vpush-ima.exports (100.64.0.0/10). Do not bind 0.0.0.0.
#
#   sudo install -m 644 nfsd-tailscale.conf /etc/nfs.conf.d/vpush-bind.conf
#   sudo systemctl restart nfs-server
#
[nfsd]
host=100.112.25.21
